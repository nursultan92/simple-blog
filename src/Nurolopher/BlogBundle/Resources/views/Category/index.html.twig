{% extends '::base.html.twig' %}
{% block body %}
    <a href="{{ path('nurolopher_blog_category_new') }}" class="btn btn-primary pull-right">Create new</a>
    <table class="table">
        <thead>
        <tr>
            <th>Number</th>
            <th>Title</th>
            <th>Action</th>
            <th>Action</th>
        </tr>
        </thead>
        {% for category in categories %}
            <tr>
                <td><a href="{{ path('nurolopher_blog_category_edit',{'id':category.id}) }}">{{ loop.index }}</a></td>
                <td>{{ category.title }}</td>
                <td><a href="{{ path('nurolopher_blog_category_edit',{'id':category.id}) }}">Edit</a></td>
                <td><a href="{{ path('nurolopher_blog_category_delete',{id: category.id}) }}">Delete</a></td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}